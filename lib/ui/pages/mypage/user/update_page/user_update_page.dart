import 'package:flutter/material.dart';

class UserUpdatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('UserUpdatePage')),
    );
  }
}
