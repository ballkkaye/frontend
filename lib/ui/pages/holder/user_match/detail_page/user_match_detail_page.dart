import 'package:flutter/material.dart';

class UserMatchDetailPage extends StatelessWidget {
  const UserMatchDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("동행 상세보기 페이지"),
        ),
      ),
    );
  }
}
